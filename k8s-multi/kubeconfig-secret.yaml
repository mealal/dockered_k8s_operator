# =============================================================================
# Multi-Cluster Kubeconfig Secret
# =============================================================================
#
# This secret contains the kubeconfig file that allows the MongoDB Enterprise
# Kubernetes Operator to access and manage resources across multiple clusters.
#
# Apply with: kubectl apply -f kubeconfig-secret.yaml (central cluster only)
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: mongodb-enterprise-operator-multi-cluster-kubeconfig
  namespace: mongodb
  labels:
    app.kubernetes.io/name: mongodb-enterprise-operator
    app.kubernetes.io/component: kubeconfig
    app.kubernetes.io/managed-by: deploy-mongodb-k8s-multi
type: Opaque
data:
  kubeconfig: {{KUBECONFIG_DATA}}
